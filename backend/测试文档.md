# 测试文档

> 接口：`POST /api/sql/execute`
> 地址：`http://localhost:8080/swagger-ui/index.html`
> 请求体格式：`{ "databaseName": "数据库名", "sql": "SQL语句" }`

---

## 测试集 1：用户管理

---

### 1.1 创建用户

**请求：**
```json
{ "databaseName": "", "sql": "CREATE USER 'alice' IDENTIFIED BY 'alice123'" }
```
**实际结果：**
```
{
  "code": 200,
  "message": "执行成功",
  "data": {
    "type": "message",
    "status": "success",
    "data": "用户创建成功",
    "affectedRows": 1,
    "refreshTree": false
  }
}
```

---

### 1.2 重复创建用户（预期失败）

**请求：**
```json
{ "databaseName": "", "sql": "CREATE USER 'alice' IDENTIFIED BY 'xxx'" }
```
**实际结果：**
```
{
  "code": 400,
  "message": "用户已存在",
  "data": null
}
```

---

### 1.3 删除不存在的用户（预期失败）

**请求：**
```json
{ "databaseName": "", "sql": "DROP USER 'nonexist'" }
```
**实际结果：**
```
{
  "code": 400,
  "message": "用户不存在: nonexist",
  "data": null
}
```

---

### 1.4 删除管理员（预期失败）

**请求：**
```json
{ "databaseName": "", "sql": "DROP USER 'admin'" }
```
**实际结果：**
```
{
  "code": 400,
  "message": "不能删除管理员账户",
  "data": null
}
```

---

### 1.5 修改密码

**请求：**
```json
{ "databaseName": "", "sql": "ALTER USER 'alice' IDENTIFIED BY 'newpwd'" }
```
**实际结果：**
```
{
  "code": 200,
  "message": "执行成功",
  "data": {
    "type": "message",
    "status": "success",
    "data": "用户修改成功",
    "affectedRows": 1,
    "refreshTree": false
  }
}
```

---

### 1.6 删除用户

**请求：**
```json
{ "databaseName": "", "sql": "DROP USER 'alice'" }
```
**实际结果：**
```
{
  "code": 200,
  "message": "执行成功",
  "data": {
    "type": "message",
    "status": "success",
    "data": "用户删除成功",
    "affectedRows": 1,
    "refreshTree": false
  }
}
```

---

### 1.7 持久化验证（重启应用）

重启 Spring Boot 应用后，用默认管理员登录：

**请求：**
```json
{ "databaseName": "", "sql": "CONNECT TO mydb PORT 3306 USER 'admin' IDENTIFIED BY 'admin123'" }
```
**实际结果：**
```
{
  "code": 200,
  "message": "执行成功",
  "data": {
    "type": "message",
    "status": "success",
    "data": "连接成功",
    "clientId": "d476ba62-e9e4-4d21-b2af-66c27e6a9822",
    "username": "admin",
    "token": "YWRtaW46ZGJtcw==",
    "affectedRows": 1,
    "refreshTree": false
  }
}
```

---

## 测试集 2：认证与会话

---

### 2.1 管理员登录

**请求：**
```json
{ "databaseName": "", "sql": "CONNECT TO mydb PORT 3306 USER 'admin' IDENTIFIED BY 'admin123'" }
```
**实际结果：**
```
{
  "code": 200,
  "message": "执行成功",
  "data": {
    "type": "message",
    "status": "success",
    "data": "连接成功",
    "clientId": "54f3c5f1-330d-4b9d-b9fa-8d54f4b6935b",
    "username": "admin",
    "token": "YWRtaW46ZGJtcw==",
    "affectedRows": 1,
    "refreshTree": false
  }
}
```

---

### 2.2 错误密码（预期失败）

**请求：**
```json
{ "databaseName": "", "sql": "CONNECT TO mydb PORT 3306 USER 'admin' IDENTIFIED BY 'wrong'" }
```
**实际结果：**
```
{
  "code": 400,
  "message": "用户名或密码错误",
  "data": null
}
```

---

### 2.3 查询在线客户端

先创建一个登录会话后执行：

**请求：**
```json
{ "databaseName": "", "sql": "SHOW CLIENTS" }
```
**实际结果：**
```
{
  "code": 200,
  "message": "执行成功",
  "data": {
    "type": "table",
    "status": "success",
    "columns": [
      {
        "prop": "CLIENT_ID",
        "label": "CLIENT_ID"
      },
      {
        "prop": "USER",
        "label": "USER"
      },
      {
        "prop": "IP_ADDRESS",
        "label": "IP_ADDRESS"
      },
      {
        "prop": "PORT",
        "label": "PORT"
      },
      {
        "prop": "CONNECTED_AT",
        "label": "CONNECTED_AT"
      },
      {
        "prop": "CURRENT_DATABASE",
        "label": "CURRENT_DATABASE"
      }
    ],
    "data": [
      {
        "CLIENT_ID": "d476ba62-e9e4-4d21-b2af-66c27e6a9822",
        "USER": "admin",
        "IP_ADDRESS": "",
        "PORT": 0,
        "CONNECTED_AT": "2026-05-09T11:07:54.814130600Z",
        "CURRENT_DATABASE": ""
      },
      {
        "CLIENT_ID": "54f3c5f1-330d-4b9d-b9fa-8d54f4b6935b",
        "USER": "admin",
        "IP_ADDRESS": "",
        "PORT": 0,
        "CONNECTED_AT": "2026-05-09T11:08:11.069851900Z",
        "CURRENT_DATABASE": ""
      }
    ],
    "affectedRows": 2
  }
}
```

---

### 2.4 断开连接

将 2.1 返回的 clientId（如 `54f3c5f1-330d-4b9d-b9fa-8d54f4b6935b`）填入 SQL：

**请求：**
```json
{ "databaseName": "", "sql": "DISCONNECT 54f3c5f1-330d-4b9d-b9fa-8d54f4b6935b" }
```
**实际结果：**
```
{
  "code": 200,
  "message": "执行成功",
  "data": {
    "type": "message",
    "status": "success",
    "data": "连接已断开",
    "affectedRows": 1,
    "refreshTree": false
  }
}
```

---

### 2.5 确认已断开

**请求：**
```json
{ "databaseName": "", "sql": "SHOW CLIENTS" }
```
**实际结果：**
```
{
  "code": 200,
  "message": "执行成功",
  "data": {
    "type": "table",
    "status": "success",
    "columns": [
      {
        "prop": "CLIENT_ID",
        "label": "CLIENT_ID"
      },
      {
        "prop": "USER",
        "label": "USER"
      },
      {
        "prop": "IP_ADDRESS",
        "label": "IP_ADDRESS"
      },
      {
        "prop": "PORT",
        "label": "PORT"
      },
      {
        "prop": "CONNECTED_AT",
        "label": "CONNECTED_AT"
      },
      {
        "prop": "CURRENT_DATABASE",
        "label": "CURRENT_DATABASE"
      }
    ],
    "data": [],
    "affectedRows": 0
  }
}
```

---

## 测试集 3：权限管理

---

### 前置：创建测试用户

**请求：**
```json
{ "databaseName": "", "sql": "CREATE USER 'bob' IDENTIFIED BY 'bob123'" }
```
**实际结果：**
```
{
  "code": 200,
  "message": "执行成功",
  "data": {
    "type": "message",
    "status": "success",
    "data": "用户创建成功",
    "affectedRows": 1,
    "refreshTree": false
  }
}
```

---

### 3.1 授予权限

**请求：**
```json
{ "databaseName": "", "sql": "GRANT SELECT, INSERT ON mydb.* TO 'bob'" }
```
**实际结果：**
```
{
  "code": 200,
  "message": "执行成功",
  "data": {
    "type": "message",
    "status": "success",
    "data": "权限授予成功",
    "affectedRows": 1,
    "refreshTree": false
  }
}
```

---

### 3.2 查询权限

**请求：**
```json
{ "databaseName": "", "sql": "SHOW GRANTS FOR 'bob'" }
```
**实际结果：**
```
{
  "code": 200,
  "message": "执行成功",
  "data": {
    "type": "table",
    "status": "success",
    "columns": [
      {
        "prop": "USER",
        "label": "USER"
      },
      {
        "prop": "GRANTS",
        "label": "GRANTS"
      }
    ],
    "data": [
      {
        "USER": "bob",
        "GRANTS": [
          "SELECT ON mydb.*",
          "INSERT ON mydb.*"
        ]
      }
    ],
    "affectedRows": 1
  }
}
```

---

### 3.3 撤销权限

**请求：**
```json
{ "databaseName": "", "sql": "REVOKE INSERT ON mydb.* FROM 'bob'" }
```
**实际结果：**
```
{
  "code": 200,
  "message": "执行成功",
  "data": {
    "type": "message",
    "status": "success",
    "data": "权限撤销成功",
    "affectedRows": 1,
    "refreshTree": false
  }
}
```

---

### 3.4 确认已撤销

**请求：**
```json
{ "databaseName": "", "sql": "SHOW GRANTS FOR 'bob'" }
```
**实际结果：**
```
{
  "code": 200,
  "message": "执行成功",
  "data": {
    "type": "table",
    "status": "success",
    "columns": [
      {
        "prop": "USER",
        "label": "USER"
      },
      {
        "prop": "GRANTS",
        "label": "GRANTS"
      }
    ],
    "data": [
      {
        "USER": "bob",
        "GRANTS": [
          "SELECT ON mydb.*"
        ]
      }
    ],
    "affectedRows": 1
  }
}
```

---

### 3.5 授予全部权限

**请求：**
```json
{ "databaseName": "", "sql": "GRANT ALL PRIVILEGES ON *.* TO 'bob'" }
```
**实际结果：**
```
{
  "code": 200,
  "message": "执行成功",
  "data": {
    "type": "message",
    "status": "success",
    "data": "权限授予成功",
    "affectedRows": 1,
    "refreshTree": false
  }
}
```

---

## 测试集 4：事务与回滚

> 前置条件：需要先有数据库和表

### 前置准备

**创建数据库：**
```json
{ "databaseName": "", "sql": "CREATE DATABASE testdb" }
```
**实际结果：**
```
{
  "code": 200,
  "message": "执行成功",
  "data": {
    "type": "message",
    "status": "success",
    "data": "数据库创建成功",
    "affectedRows": 1,
    "refreshTree": true
  }
}
```

**创建测试表：**
```json
{ "databaseName": "testdb", "sql": "CREATE TABLE items (id INT, name VARCHAR(50))" }
```
**实际结果：**
```
{
  "code": 200,
  "message": "执行成功",
  "data": {
    "type": "message",
    "status": "success",
    "data": "表创建成功",
    "affectedRows": 0,
    "refreshTree": true
  }
}
```

---

### 4.1 INSERT 回滚

**BEGIN：**
```json
{ "databaseName": "testdb", "sql": "BEGIN" }
```
**实际结果：**
```
{
  "code": 200,
  "message": "执行成功",
  "data": {
    "type": "message",
    "status": "success",
    "data": "事务已开启: TESTDB-b0dd5f92-d7ad-4cd8-b4a8-535ad6d5319c",
    "affectedRows": 0,
    "refreshTree": false
  }
}
```

**INSERT 第一条：**
```json
{ "databaseName": "testdb", "sql": "INSERT INTO items VALUES (1, '苹果')" }
```
**实际结果：**
```
{
  "code": 200,
  "message": "执行成功",
  "data": {
    "type": "message",
    "status": "success",
    "data": "插入成功",
    "affectedRows": 1,
    "refreshTree": false
  }
}
```

**INSERT 第二条：**
```json
{ "databaseName": "testdb", "sql": "INSERT INTO items VALUES (2, '香蕉')" }
```
**实际结果：**
```
{
  "code": 200,
  "message": "执行成功",
  "data": {
    "type": "message",
    "status": "success",
    "data": "插入成功",
    "affectedRows": 1,
    "refreshTree": false
  }
}
```

**查询确认：**
```json
{ "databaseName": "testdb", "sql": "SELECT * FROM items" }
```
**实际结果：**
```
{
  "code": 200,
  "message": "执行成功",
  "data": {
    "type": "table",
    "status": "success",
    "columns": [
      {
        "prop": "ID",
        "label": "ID"
      },
      {
        "prop": "NAME",
        "label": "NAME"
      }
    ],
    "data": [
      {
        "ID": 1,
        "NAME": "苹果"
      },
      {
        "ID": 2,
        "NAME": "香蕉"
      }
    ],
    "affectedRows": 2
  }
}
```

**ROLLBACK：**
```json
{ "databaseName": "testdb", "sql": "ROLLBACK" }
```
**实际结果：**
```
{
  "code": 200,
  "message": "执行成功",
  "data": {
    "type": "message",
    "status": "success",
    "data": "事务已回滚",
    "affectedRows": 0,
    "refreshTree": false
  }
}
```

**回滚后查询：**
```json
{ "databaseName": "testdb", "sql": "SELECT * FROM items" }
```
**实际结果：**
```
{
  "code": 200,
  "message": "执行成功",
  "data": {
    "type": "table",
    "status": "success",
    "columns": [
      {
        "prop": "ID",
        "label": "ID"
      },
      {
        "prop": "NAME",
        "label": "NAME"
      }
    ],
    "data": [],
    "affectedRows": 0
  }
}
```

---

### 4.2 UPDATE 回滚（INSERT + UPDATE 在同一事务内）

**BEGIN：**
```json
{ "databaseName": "testdb", "sql": "BEGIN" }
```
**实际结果：**
```
{
  "code": 200,
  "message": "执行成功",
  "data": {
    "type": "message",
    "status": "success",
    "data": "事务已开启: TESTDB-479ff1c4-5b3e-48f3-be7d-c2610101c222",
    "affectedRows": 0,
    "refreshTree": false
  }
}
```

**INSERT 测试数据：**
```json
{ "databaseName": "testdb", "sql": "INSERT INTO items VALUES (1, '苹果')" }
```
**实际结果：**
```
{
  "code": 200,
  "message": "执行成功",
  "data": {
    "type": "message",
    "status": "success",
    "data": "插入成功",
    "affectedRows": 1,
    "refreshTree": false
  }
}
```

**UPDATE：**
```json
{ "databaseName": "testdb", "sql": "UPDATE items SET name = '西瓜' WHERE id = 1" }
```
**实际结果：**
```
{
  "code": 200,
  "message": "执行成功",
  "data": {
    "type": "message",
    "status": "success",
    "data": "更新成功",
    "affectedRows": 1,
    "refreshTree": false
  }
}
```

**查询确认已更新：**
```json
{ "databaseName": "testdb", "sql": "SELECT * FROM items" }
```
**实际结果：**
```
{
  "code": 200,
  "message": "执行成功",
  "data": {
    "type": "table",
    "status": "success",
    "columns": [
      {
        "prop": "ID",
        "label": "ID"
      },
      {
        "prop": "NAME",
        "label": "NAME"
      }
    ],
    "data": [
      {
        "ID": 1,
        "NAME": "西瓜"
      }
    ],
    "affectedRows": 1
  }
}
```

**ROLLBACK：**
```json
{ "databaseName": "testdb", "sql": "ROLLBACK" }
```
**实际结果：**
```
{
  "code": 200,
  "message": "执行成功",
  "data": {
    "type": "message",
    "status": "success",
    "data": "事务已回滚",
    "affectedRows": 0,
    "refreshTree": false
  }
}
```

**回滚后查询（事务内全部撤销，应为空表）：**
```json
{ "databaseName": "testdb", "sql": "SELECT * FROM items" }
```
**实际结果：**
```
{
  "code": 200,
  "message": "执行成功",
  "data": {
    "type": "table",
    "status": "success",
    "columns": [
      {
        "prop": "ID",
        "label": "ID"
      },
      {
        "prop": "NAME",
        "label": "NAME"
      }
    ],
    "data": [],
    "affectedRows": 0
  }
}
```

---

### 4.3 DELETE 回滚（INSERT + DELETE 在同一事务内）

**BEGIN：**
```json
{ "databaseName": "testdb", "sql": "BEGIN" }
```
**实际结果：**
```
{
  "code": 200,
  "message": "执行成功",
  "data": {
    "type": "message",
    "status": "success",
    "data": "事务已开启: TESTDB-24c8e46e-7bdb-41df-8c95-4747893464e9",
    "affectedRows": 0,
    "refreshTree": false
  }
}
```

**INSERT 测试数据：**
```json
{ "databaseName": "testdb", "sql": "INSERT INTO items VALUES (1, '苹果')" }
```
**实际结果：**
```
{
  "code": 200,
  "message": "执行成功",
  "data": {
    "type": "message",
    "status": "success",
    "data": "插入成功",
    "affectedRows": 1,
    "refreshTree": false
  }
}
```

**DELETE：**
```json
{ "databaseName": "testdb", "sql": "DELETE FROM items WHERE id = 1" }
```
**实际结果：**
```
{
  "code": 200,
  "message": "执行成功",
  "data": {
    "type": "message",
    "status": "success",
    "data": "删除成功",
    "affectedRows": 1,
    "refreshTree": false
  }
}
```

**查询确认已删除：**
```json
{ "databaseName": "testdb", "sql": "SELECT * FROM items" }
```
**实际结果：**
```
{
  "code": 200,
  "message": "执行成功",
  "data": {
    "type": "table",
    "status": "success",
    "columns": [
      {
        "prop": "ID",
        "label": "ID"
      },
      {
        "prop": "NAME",
        "label": "NAME"
      }
    ],
    "data": [],
    "affectedRows": 0
  }
}
```

**ROLLBACK：**
```json
{ "databaseName": "testdb", "sql": "ROLLBACK" }
```
**实际结果：**
```
{
  "code": 200,
  "message": "执行成功",
  "data": {
    "type": "message",
    "status": "success",
    "data": "事务已回滚",
    "affectedRows": 0,
    "refreshTree": false
  }
}
```

**回滚后查询（事务内全部撤销，应为空表）：**
```json
{ "databaseName": "testdb", "sql": "SELECT * FROM items" }
```
**实际结果：**
```
{
  "code": 200,
  "message": "执行成功",
  "data": {
    "type": "table",
    "status": "success",
    "columns": [
      {
        "prop": "ID",
        "label": "ID"
      },
      {
        "prop": "NAME",
        "label": "NAME"
      }
    ],
    "data": [],
    "affectedRows": 0
  }
}
```

---

### 4.4 COMMIT 确认

**BEGIN：**
```json
{ "databaseName": "testdb", "sql": "BEGIN" }
```
**实际结果：**
```
{
  "code": 200,
  "message": "执行成功",
  "data": {
    "type": "message",
    "status": "success",
    "data": "事务已开启: TESTDB-7dcaa49d-f9ca-4f10-b37b-e8a55622ba6a",
    "affectedRows": 0,
    "refreshTree": false
  }
}
```

**INSERT：**
```json
{ "databaseName": "testdb", "sql": "INSERT INTO items VALUES (99, '持久化测试')" }
```
**实际结果：**
```
{
  "code": 200,
  "message": "执行成功",
  "data": {
    "type": "message",
    "status": "success",
    "data": "插入成功",
    "affectedRows": 1,
    "refreshTree": false
  }
}
```

**COMMIT：**
```json
{ "databaseName": "testdb", "sql": "COMMIT" }
```
**实际结果：**
```
{
  "code": 200,
  "message": "执行成功",
  "data": {
    "type": "message",
    "status": "success",
    "data": "事务已提交",
    "affectedRows": 0,
    "refreshTree": false
  }
}
```

**提交后查询（数据应保留）：**
```json
{ "databaseName": "testdb", "sql": "SELECT * FROM items" }
```
**实际结果：**
```
{
  "code": 200,
  "message": "执行成功",
  "data": {
    "type": "table",
    "status": "success",
    "columns": [
      {
        "prop": "ID",
        "label": "ID"
      },
      {
        "prop": "NAME",
        "label": "NAME"
      }
    ],
    "data": [
      {
        "ID": 99,
        "NAME": "持久化测试"
      }
    ],
    "affectedRows": 1
  }
}
```

**再次 ROLLBACK（不应影响已提交数据）：**
```json
{ "databaseName": "testdb", "sql": "ROLLBACK" }
```
**实际结果：**
```
{
  "code": 200,
  "message": "执行成功",
  "data": {
    "type": "message",
    "status": "success",
    "data": "事务已回滚",
    "affectedRows": 0,
    "refreshTree": false
  }
}
```

---

## 测试集 5：边界与异常

---

### 5.1 重复创建 admin（预期失败）

**请求：**
```json
{ "databaseName": "", "sql": "CREATE USER 'admin' IDENTIFIED BY 'x'" }
```
**实际结果：**
```
{
  "code": 400,
  "message": "用户已存在",
  "data": null
}
```

---

### 5.2 未指定数据库执行 BEGIN（预期失败）

**请求：**
```json
{ "databaseName": "", "sql": "BEGIN" }
```
**实际结果：**
```
{
  "code": 400,
  "message": "请先指定数据库",
  "data": null
}
```

---

### 5.3 不存在的数据库执行 BEGIN（预期失败）

**请求：**
```json
{ "databaseName": "nonexist", "sql": "BEGIN" }
```
**实际结果：**
```
{
  "code": 400,
  "message": "数据库不存在: nonexist",
  "data": null
}
```

---

### 5.4 无事务时 COMMIT（预期静默忽略）

**请求：**
```json
{ "databaseName": "testdb", "sql": "COMMIT" }
```
**实际结果：**
```
{
  "code": 200,
  "message": "执行成功",
  "data": {
    "type": "message",
    "status": "success",
    "data": "事务已提交",
    "affectedRows": 0,
    "refreshTree": false
  }
}
```

---

### 5.5 无事务时 ROLLBACK（预期静默忽略）

**请求：**
```json
{ "databaseName": "testdb", "sql": "ROLLBACK" }
```
**实际结果：**
```
{
  "code": 200,
  "message": "执行成功",
  "data": {
    "type": "message",
    "status": "success",
    "data": "事务已回滚",
    "affectedRows": 0,
    "refreshTree": false
  }
}
```

---

### 5.6 已删除用户无法登录

先用 alice 用户登录（alice 已在 1.6 中被删除，预期登录失败）：

**请求：**
```json
{ "databaseName": "", "sql": "CONNECT TO mydb PORT 3306 USER 'alice' IDENTIFIED BY 'alice123'" }
```
**实际结果：**
```
{
  "code": 400,
  "message": "用户名或密码错误",
  "data": null
}
```
