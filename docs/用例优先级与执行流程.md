# DBMS 项目最终用例优先级与执行流程

## 1. 排序原则
用例优先级按照以下原则排序：
- 业务价值高：直接影响系统可演示性和答辩主线。
- 实现成本低：依赖少、路径短、联调风险可控。
- 可扩展性强：先实现后能承接更多功能。

## 2. 用例清单（按优先级）

| 优先级 | 用例名称 | 价值与难度判断 | 主要涉及模块 |
| --- | --- | --- | --- |
| P0 | 服务健康检查 | 价值高、实现最简单，是联调起点 | 前端 `api/http.js`、后端 `controller/HealthController`、`common/ApiResponse` |
| P1 | 查询数据库列表 | 核心入口，高价值、低难度 | `DatabaseController`、`DatabaseApplicationService`、`MetadataService`、`DatabaseMetaRepository` |
| P1 | 创建数据库 | 核心主流程，高价值、难度中低 | `DatabaseController`、`DatabaseApplicationService`、`MetadataService`、`DatabaseMetaRepository`、`LogRepository` |
| P1 | 删除数据库 | 核心主流程，高价值、难度中低 | 同上，外加目录清理逻辑与会话校验 |
| P2 | 查询表列表 | 结构管理入口，难度中低 | `TableApplicationService`、`MetadataService`、`TableMetaRepository` |
| P2 | 创建表 | 结构管理核心，难度中 | `TableApplicationService`、`MetadataService`、`SchemaRepository`、`IndexRepository` |
| P2 | 删除表 | 结构管理核心，难度中 | `TableApplicationService`、`MetadataService`、`RecordRepository`、`IndexRepository` |
| P3 | 条件查询记录 | 数据主流程，答辩重点 | `RecordApplicationService`、`RecordService`、`IndexService`、`RecordRepository` |
| P3 | 插入记录 | 数据主流程，约束链路完整 | `RecordApplicationService`、`IntegrityService`、`IndexService`、`RecordRepository` |
| P3 | 更新/删除记录 | 数据主流程，影响索引与完整性 | `RecordApplicationService`、`IntegrityService`、`IndexService`、`RecordRepository` |
| P4 | 创建/查询索引 | 性能能力，课程要求必做 | `IndexApplicationService`、`IndexService`、`IndexRepository` |
| P4 | 完整性校验 | 数据正确性核心，课程要求必做 | `IntegrityApplicationService`、`IntegrityService`、`SchemaRepository`、`RecordRepository` |
| P5 | 登录/登出与权限校验 | 安全能力，答辩加分 | `SecurityApplicationService`、`SecurityService`、`UserRepository` |
| P5 | 数据库备份/恢复 | 维护能力，答辩加分 | `BackupApplicationService`、`BackupService`、`BackupRepository`、`LogRepository` |
| P6 | 事务开启/提交/回滚 | 难度高，放后期完善 | `TransactionApplicationService`、`TransactionService`、`LogRepository` |
| P6 | 客户端连接管理 | 扩展能力，非第一阶段主线 | `ClientApplicationService`、`ClientService`、`NetworkAdapter` |

## 3. 各用例执行流程（从前端到后端）

### 3.1 用例：服务健康检查（P0）
1. 用户进入前端页面并点击“检查服务状态”。
2. 前端页面调用 `api/http.js` 发送 `GET /api/health`。
3. 后端 `HealthController` 接收请求。
4. 控制器构建服务状态信息（状态、时间戳）。
5. 统一封装为 `ApiResponse` 返回。
6. 前端接收结果并渲染状态提示。

### 3.2 用例：查询数据库列表（P1）
1. 用户打开数据库管理页。
2. 前端发起 `GET /api/databases` 请求。
3. `DatabaseController` 接收请求并调用 `DatabaseApplicationService.listDatabases`。
4. 应用层调用 `MetadataService` 读取数据库元信息。
5. 业务层调用 `DatabaseMetaRepository.readAllDatabaseMeta` 从描述文件读取数据。
6. 结果逐层返回到控制器并封装 `ApiResponse`。
7. 前端展示数据库列表。

### 3.3 用例：创建数据库（P1）
1. 用户在前端输入数据库名称并点击“创建”。
2. 前端做基础校验（非空、命名规则）后发起 `POST /api/databases`。
3. `DatabaseController` 绑定请求参数并调用应用层。
4. 应用层执行流程编排：名称校验 -> 重名检查 -> 创建目录 -> 写元数据。
5. 业务层通过 `MetadataService` 调用 `DatabaseMetaRepository.appendDatabaseMeta` 写入描述文件。
6. 可选写入操作日志（`LogRepository`）。
7. 返回成功结果，前端刷新列表并提示创建成功。

### 3.4 用例：删除数据库（P1）
1. 用户在数据库列表点击“删除”。
2. 前端二次确认后发起 `DELETE /api/databases/{databaseName}`。
3. 控制器调用应用层删除流程。
4. 应用层检查数据库是否存在、是否被会话占用。
5. 业务层调用仓储删除元数据并清理关联目录/文件。
6. 记录操作日志并返回结果。
7. 前端刷新列表并显示执行结果。

### 3.5 用例：查询表列表（P2）
1. 用户进入某数据库下的表管理页。
2. 前端发起 `GET /api/databases/{databaseName}/tables`。
3. 控制器调用 `TableApplicationService.listTables`。
4. 应用层调用 `MetadataService`。
5. 业务层调用 `TableMetaRepository.readAllTableMeta`。
6. 返回表列表，前端渲染表信息。

### 3.6 用例：创建表（P2）
1. 用户填写表名、字段、约束并提交。
2. 前端发送 `POST /api/databases/{databaseName}/tables`。
3. 控制器接收请求，应用层开始创建流程。
4. 应用层调用业务层保存表元信息与字段定义。
5. `TableMetaRepository`、`SchemaRepository` 持久化结构文件。
6. 初始化记录文件与索引描述文件。
7. 返回结果，前端跳转或刷新表列表。

### 3.7 用例：删除表（P2）
1. 用户点击删除表。
2. 前端发起 `DELETE /api/databases/{databaseName}/tables/{tableName}`。
3. 控制器调用应用层删除流程。
4. 应用层协调业务层删除表元数据、记录文件、索引文件、约束文件。
5. 仓储层清理相关文件并返回状态。
6. 前端刷新表列表并提示结果。

### 3.8 用例：条件查询记录（P3）
1. 用户输入查询条件。
2. 前端发送 `POST /records/query`。
3. 控制器调用 `RecordApplicationService.queryRecords`。
4. 应用层判断是否有可用索引。
5. 有索引则调用 `IndexService` 先定位记录指针；否则全表扫描。
6. `RecordService` 调用 `RecordRepository.scan` 获取记录。
7. 返回数据列表，前端以表格展示。

### 3.9 用例：插入记录（P3）
1. 用户填写记录并提交。
2. 前端发送 `POST /records`。
3. 控制器调用应用层插入流程。
4. 应用层调用 `IntegrityService` 执行主键、非空、唯一等检查。
5. 校验通过后 `RecordService` 写入记录文件。
6. `IndexService` 同步更新索引。
7. 返回成功，前端提示并刷新记录列表。

### 3.10 用例：更新/删除记录（P3）
1. 用户选择记录并执行更新或删除。
2. 前端发送 `PUT /records` 或 `DELETE /records`。
3. 控制器调用应用层。
4. 更新时先完整性校验，再执行写入；删除时按条件定位并删除。
5. 执行后统一同步索引更新。
6. 返回执行条数和结果信息。
7. 前端更新列表。

### 3.11 用例：创建/查询索引（P4）
1. 用户在表管理页触发索引操作。
2. 前端发送创建或查询请求。
3. 控制器调用 `IndexApplicationService`。
4. 业务层 `IndexService` 构建或读取索引结构。
5. `IndexRepository` 读写索引描述与数据文件。
6. 返回索引信息，前端显示状态。

### 3.12 用例：完整性校验（P4）
1. 用户提交插入/更新操作。
2. 应用层在真正写入前调用 `IntegrityApplicationService`。
3. 业务层执行规则检查（主键、外键、唯一、非空、默认值、自增）。
4. 调用 `SchemaRepository` 和 `RecordRepository` 读取结构与已有数据。
5. 若失败则中止写入并返回详细错误。
6. 若通过则放行后续写入流程。

### 3.13 用例：登录/登出与权限校验（P5）
1. 用户输入账号密码登录。
2. 前端发送 `POST /api/auth/login`。
3. 控制器调用 `SecurityApplicationService.login`。
4. 业务层 `SecurityService` 调用 `UserRepository` 校验用户和密码。
5. 登录成功返回会话信息；失败返回错误。
6. 后续操作通过权限校验决定是否放行。

### 3.14 用例：备份与恢复（P5）
1. 用户触发备份或恢复操作。
2. 前端调用 `/api/backup/{databaseName}` 或 `/api/restore/{backupName}`。
3. 控制器调用 `BackupApplicationService`。
4. 业务层 `BackupService` 编排文件打包或解包流程。
5. `BackupRepository` 执行备份文件读写；必要时读取日志文件。
6. 返回结果供前端展示。

### 3.15 用例：事务管理（P6）
1. 前端触发批量修改时，请求开启事务。
2. 应用层调用 `TransactionService.begin`。
3. 每步操作写事务日志（`LogRepository`）。
4. 全部成功执行提交；失败执行回滚。
5. 返回事务状态与执行结果。

### 3.16 用例：客户端连接管理（P6）
1. 客户端发起连接。
2. 应用层调用 `ClientService`。
3. `NetworkAdapter` 负责连接建立、请求接收、响应发送。
4. 返回连接状态并维护会话。

## 4. 第一阶段推荐交付顺序
1. 健康检查
2. 查询数据库列表
3. 创建数据库
4. 删除数据库
5. 查询表列表
6. 创建表
7. 条件查询记录
8. 插入记录
9. 更新/删除记录

按以上顺序推进，可在最短时间内形成可演示主线，并减少联调阻塞。
