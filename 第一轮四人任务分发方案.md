# 第一轮四人任务分发方案

## 1. 第一轮总目标
- 完成前端原型（至少数据库与表管理页面可演示）。
- 定义并落地文件存储系统（数据库/表/字段/约束元数据文件）。
- 明确业务规则清单（至少覆盖数据库、表、记录的核心规则）。
- 定义前后端 RESTful API 协议并打通首批联调。

## 2. 任务拆分原则
- 先主线后增强：优先保证“查数据库 -> 建数据库 -> 删数据库”完整闭环。
- 模块边界清晰：避免多人改同一层同一路径。
- 每人有可验收输出：每项任务都能独立验收。

## 3. 四人分工

### 成员 A（后端主线）
负责内容：
- 设计并实现数据库/表管理 API 的 Controller + Application 主流程。
- 输出数据库管理三接口：查询列表、创建、删除。
- 对齐统一返回结构 `ApiResponse`。

交付物：
- 后端可运行接口。
- 接口文档 v1（路径、参数、返回示例）。
- Postman 调试集合。

验收标准：
- `GET/POST/DELETE /api/databases` 可调用。
- 错误参数有明确返回。

### 成员 B（存储与规则）
负责内容：
- 设计文件存储系统格式并落地基础读写。
- 明确并实现第一轮业务规则。

文件存储需定义：
- 数据库描述文件：记录数据库基本信息。
- 表描述文件：记录表目录与结构文件路径。
- 字段定义文件：记录字段类型、主键、默认值等。
- 约束文件：记录唯一、非空、外键等规则。

业务规则需定义：
- 数据库名合法性与唯一性。
- 表名合法性与唯一性。
- 字段名合法性与重复校验。
- 记录写入前的非空与主键校验。

交付物：
- 文件格式说明文档。
- 基础仓储读写实现。
- 规则清单文档与基础校验实现。

验收标准：
- 能创建并读取数据库与表结构元数据。
- 非法输入可被规则拦截。

### 成员 C（前端原型负责人）
负责内容：
- 输出前端页面原型并完成首批页面实现。
- 建立 UI 规范（布局、颜色、按钮、表格、弹窗）。
- 封装 API 调用模块并接通首批接口。

必须完成页面：
- 登录页
- 首页
- 数据库管理页
- 表管理页

交付物：
- 可运行前端页面。
- 页面原型图或说明文档。
- API 调用封装代码。

验收标准：
- 页面可演示完整点击链路。
- 可联调健康检查和数据库列表。

### 成员 D（协议与联调测试）
负责内容：
- 定义 RESTful API 协议规范。
- 组织前后端联调和测试闭环。

RESTful 协议需定义：
- URL 命名规范（资源化路径）。
- 请求方法规范（GET/POST/PUT/DELETE）。
- 通用状态码与错误码。
- 统一响应结构。

交付物：
- API 协议规范文档。
- 联调测试清单与结果记录。
- 问题单与回归结论。

验收标准：
- 至少 5 条核心用例联调通过。
- 每个失败场景有可复现记录。

## 4. 第一轮里程碑
- 第 1-2 天：接口协议与页面原型定稿。
- 第 3-5 天：后端数据库管理主线 + 存储规则落地。
- 第 6-7 天：前端页面联调与测试闭环。

## 5. 第一轮验收清单
- 前端原型可演示。
- 数据库管理接口可用。
- 文件存储系统定义清晰且能读写。
- 业务规则可拦截非法输入。
- API 协议文档完整并通过联调验证。
